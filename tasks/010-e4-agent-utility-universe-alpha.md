# Task 010 — E4 Agent Utility Universe Alpha

**Status:** Foundation and bounded World State admission merged on the E4
program branch; exact Opportunity policy approved; one-shot acquisition,
privacy-safe compilation and local Utility Universe release verification
complete on an isolated founder-review branch; isolated encrypted off-host
archive and byte-identical restore pass; merge and deployment pending

## Objective

Extend the protected FUTO release from atomic Semantic Packets to useful,
proof-linked Semantic Frames and domain Universes without weakening E1–E3,
inflating fixture counts, or granting models or importers authority.

```text
packet -> frame -> universe -> bounded typed query -> packet proof
```

## Protected baseline

The E4 branch starts from public FUTO release source commit
`9b077663a8d3f86d75f4ad18369d073129d81a85`. E4 work MUST NOT change or deploy
the current public website, Query Lab or released snapshot before a separate
founder review.

## Subgates

### E4.1 — Ontology and frame contracts

- language-neutral frame, mapping, module and universe CDDL;
- deterministic canonical CBOR;
- shared positive and adversarial Go/restricted-C vectors;
- exact module imports and cycle rejection;
- candidate/reviewed/admitted authority boundaries;
- semantic diff classifications;
- source-bound frame slots with no unused or unproved packet.

### E4.2 — Source-specific compiler foundations

- World Bank V2 JSON profile;
- controlled Grants.gov-shaped opportunity profile;
- evidence stored before parsing;
- strict source identity and policy-decision binding;
- duplicate, unknown, oversized and malformed input rejection;
- native lexical values and locators preserved;
- no network path in importer packages;
- no fixture counted as a real source statement.

The bounded World Bank candidate reuses only the founder-reviewed E2 route and
allowlist. It stores 36 current response spools, rejects one non-200/non-JSON
response, and compiles the 35 eligible source records into proof-linked
candidate semantics. This does not authorize the broader E4 dossier route or a
scheduler.

### E4.3 — Universe Snapshot prototypes

- posting-indexed native immutable layout;
- dependency-free column-oriented analytical baseline;
- manifest-byte digest before open;
- full restoration reconciliation;
- exact bounded queries and lazy canonical frame trace;
- reproducible build, open, query, size and allocation benchmarks.

The dependency-free compact segment candidate adds deterministic binary
dictionaries, postings, lazy frame trace and verified read-only mmap. The
100,000-frame controlled benchmark remains capacity evidence only.

No external sidecar or runtime dependency is authorized by this task.

### E4.4 — Deterministic Atlas Agent alpha

- five curated scenario plans;
- visible typed queries;
- zero network, browser, live-source or model execution authority;
- resolved frame slots link to exact packet digests;
- unavailable universes remain explicitly unresolved.

### E4.5 — Real source admission

The exact Grants.gov proposal was approved at `2026-08-12T04:33:44Z`. The
single authorized retrieval was performed and its persisted one-shot claim is
consumed. Required admission evidence includes:

- human policy decision;
- immutable source evidence;
- exact work order and policy digest;
- rejection and privacy handling;
- generated real/fixture/archive count separation;
- proof-index publication and restore test.

The local release candidate contains 1,037,679 real source-derived packets,
747,783 candidate mappings and 83,087 Opportunity frames. The public compiler
withholds eligibility prose because the approved private projection contains
contact-like text. Complete Go release verification and a deterministic
132-object restricted-C sample pass. The isolated encrypted off-host archive,
full data check, byte-identical restore, restored verification and restored
agent investigation also pass.

### E4.6 — Utility Universe release

In progress. The local candidate combines 83,087 Opportunity frames with 35
World State frames and executes a bounded NSF query plus a proof-linked
two-universe investigation with zero runtime origin, browser, live-source or
model-authority calls. Website and documentation copy are synchronized as
candidate evidence. Founder admission, off-host restoration and deployment
remain pending; broader universe and delta claims require their own evidence.

### E4.7 — WSIM Seed

Pending corpus thresholds and a separate model security admission. Model
output remains candidate-only and cannot establish canon, identity, trust,
policy or execution authority.

## Exclusions

- no production PostgreSQL on Meridian;
- no browser or arbitrary URL execution;
- no authenticated, payment, purchase, write or material action;
- no automatic canon or mapping promotion;
- no model download or model runtime dependency;
- no claim that controlled scale is a real public corpus;
- no merge or deployment without founder review.

## Completion evidence

The final E4 release report must list exact real, archive, replay and fixture
counts; source decisions; snapshot identity; commands; Go/C conformance;
benchmarks; proof completeness; unresolved risks; and founder admission state.
