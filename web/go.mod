// The public website is built by a separate module so that the protocol
// module's package list, test events, and coverage record stay unchanged.
//
// This module has no requirements. It uses the Go standard library only.
module twirx.local/web

go 1.23.0
