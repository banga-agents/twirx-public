# E4.0 Ontology Fabric conformance

`vectors.tsv` is generated by `cmd/twirx-e4-vectors`. The Go data-plane codec
and the independently implemented restricted-C verifier must accept the same
seven canonical frame, mapping, module and universe objects and reject all
malformed or adversarial variants.

The corpus is offline and contains no source retrieval or personal data.
